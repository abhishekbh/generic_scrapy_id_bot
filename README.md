Generic implementation of Scrapy with a bot.

To use:
0. pip install scrapy
1. Update ./id_bot/spiders/generic_bot.py with relevant domain, urls, ids.
2. In parent directory, run ''
3. scrapy crawl generic
